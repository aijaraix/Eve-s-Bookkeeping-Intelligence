# 09 — Gemini Execution Directive

## Active owner handoff — 2026-09-15 frontend/runtime visibility repair

**Status: documented repair requirements, not an implemented fix or acceptance certificate.** Execute this scoped work order when the owner submits it for implementation. Merely reading, committing, or merging documentation does not authorize starting companies, Academy, or professional approval.

The owner reports that the existing Google development/shared application shows an AeroTech canary, no production clients, and empty financial metrics even though the Zeabur backend contains the completed Pfizer continuation and its draft reports. Independent inspection found both environment-connection uncertainty and concrete query/identity/report-projection defects. Do not dismiss this as the owner opening the wrong link.

### Read first and preserve scope

Read this active section, [06](06_UI_DATA_LINEAGE_AND_PRODUCT_AUDIT.md) including its amendment, [23](23_RUNTIME_AUTHORITY_DEPLOYMENT_AND_ENVIRONMENT_PARITY.md) including section 16, and [the dated repair evidence and acceptance matrix](FRONTEND_RUNTIME_REPAIR_2026-09-15.md). Continue to obey the existing custody, tenant, authority, physical-proof, and reporting-governance rules in this directory.

This is a continuation of the existing implementation, NOT a new architecture package. The active section bounds the broader program directive retained below. It does not weaken any security, evidence, human-signoff, or truth-governance invariant.

### Owner outcome

In the SAME existing Eve frontend, authorized users must see the actual backend work in Google AI Studio development preview, the actual Google shared/published application, and the Zeabur-hosted application when each is explicitly connected in LIVE mode. Backend extraction, evidence, specialist findings, report versions, and legitimately measured learning/governance states must be represented accurately. Compatible frontends may differ in build version, but must not silently read different production data.

No replacement dashboard, hardcoded Pfizer card, copied database, seeded data, generated trace, or direct-file-download workaround can satisfy this requirement.

### Phase 1 — Inspect current state before editing

1. Fetch current GitHub main and the documentation branch/ref supplied by the owner. Compare the actual AI Studio workspace with current remote source. Preserve legitimate newer changes; do not push an older Studio snapshot over main. Record documentation commit separately from runtime source commit.
2. Inspect the actual core/worker builds, storage identity, completed Pfizer job and continuation, and Academy/cohort posture using authorized read-only access. Treat the dated repair document as a checkpoint to verify, not a current-state guarantee.
3. Capture the actual browser origin and network destination for Studio preview, Google shared/published UI, and Zeabur UI separately. An editor address is not proof of iframe origin or backend routing. Resolve the authenticated user/tenant on the server; do not infer identity from UI titles or x-user-email.
4. Reproduce the route, storage, ID, response-schema, latest-report, silent-error, and misleading-status failures listed in the repair document. Do not navigate a GET endpoint that seeds synthetic business records until its read-side effects are isolated/repaired.
5. Return a short factual checkpoint naming the first failure, affected files, expected versus observed result, and smallest repair. Do not turn the checkpoint into another architecture audit or ask the owner to recreate the existing intake.

### Phase 2 — Repair only the demonstrated integration gaps

- Use one canonical storage resolver and a customer read model derived from actual workspace, job, continuation, evidence, and artifact/version records. Do not create a second accounting database or independent customer-status writer.
- Keep workspace IDs distinct from engagement/client IDs. Correct the detail route and `facts` response contract. Make Clients, Engagements, Documents, Overview, financial views, findings, and report lists resolve the same saved scope.
- Correct the source of reporting period, selected draft version, review limitations, and delivery status. Do not let a legacy package reader turn a FY 2024 draft into FY 2025/PUBLISHED/ENGAGEMENT_COMPLETE with fabricated clearances or scores.
- Make the central request layer cover direct component requests, downloads, and refresh connections. Establish a supported, authenticated shared-backend connection for each Google surface. A narrow server-side proxy is acceptable only with fixed upstream, controlled routes/methods, verified user/tenant context, and appropriate protections; an open proxy is not.
- Start visibility testing in LIVE_READ_ONLY mode. Google frontend/proxy processes must not initialize a production scheduler, queue writer, Academy worker, local production store, or seeder. Do not replace missing connection configuration with fixtures.
- Show connection, session, authorization, schema, and stale-data failures distinctly. Prevent late responses or cached data from a previously selected tenant/workspace from rendering under a new scope.
- Prefer/preserve the real authorized customer selection instead of silently opening a canary. Keep customer and historical test/Academy counts separate at API and UI boundaries.
- Repair the contradictory external-delivery gate and unreadable structured findings without manufacturing professional approval. Show actual persisted learning candidates, evaluations, promotions and rollback history only; do not arm Academy or manufacture learning records to populate those screens.
- Make GET/read paths side-effect-free. In particular remove/isolate production invocation of `seedRealisticGroupIfEmpty` from the entity-list read path; do not generate plausible corporate relationships while opening a page.

### Phase 3 — Validate without repeating extraction or spending unneeded model credits

Use isolated local/CI fixtures for route contracts, ID mapping, error states, tenant rejection, stale-response handling, empty stores, report versioning, period/currency/scale, structured findings, learning authority, and no-read-side-effects. Tests may use clearly isolated fixtures but must never write them to production or call them browser proof.

Regression tests must fail on the old defect and pass on the intended behavior. Do not delete negative tests, relax proof rules, hardcode expected production counts into the UI, or suppress failed assertions to make a green run.

Preserve the original Company 1 intake, document, completed extraction Attempt 9, saved Lexicon receipts, prior failures, report files, and any legitimate newer data. This task is a visibility/read-model repair. It should not require rerunning models, regenerating financial evidence, re-uploading Pfizer, resetting attempts, or changing the continuation version simply to force another specialist run. If a necessary repair would invalidate substantive outputs or require new business execution, report that boundary before proceeding.

### Phase 4 — Controlled release and real browser acceptance

Create a focused implementation branch/PR from current remote state and carry these requirements forward. Record tests and a per-service deployment plan; use the existing approved release process. A linked main branch may auto-deploy, so a merge is not merely a bookkeeping action. Do not merge blind, change production source to a documentation branch, hot-patch running source, or bypass an access/safety restriction.

Verify exact frontend/backend artifact identities, storage preservation, API compatibility, one scheduler authority, and unchanged completed-job identity after release. A manually configured source SHA label is not artifact proof. Keep worker packaging/source-label findings visible, but do not expand this work into an unrelated infrastructure redesign.

Use a REAL browser on each required surface for the journey and negative checks in the repair document. The first acceptance journey is read-only: login → select existing Pfizer → inspect source/data/evidence → inspect nine specialist results and limitations → select/download the actual latest draft → refresh/reopen. Every download must match the selected version's registered hash. Do not press Generate, Retry, or Upload merely to make data appear.

After the approved repair is released, Google returns to WATCH_ONLY for business execution: observe and report; do not manually supply missing facts, stages, model results, approvals, or reports. API verification, browser verification, and full clean autonomous-company certification remain separate claims. Preserving a previously repaired Company 1 run does not retroactively make it a clean end-to-end run under one release.

### Explicit prohibitions and deferred work

Do not restart/rebuild the whole project or create B4.4/B5. Do not start Company 2 or the remaining cohort. Do not arm Academy, create Case 6, certify the company, approve delivery, send external messages, contact clients, file government documents, or change financial accounts. Do not delete failed runs or manufacture replacement evidence.

Credential rotation is deferred by the owner and is not this work order's main task. Nevertheless, do not expose existing secrets, put service/provider credentials in client-exposed variables, weaken authentication, or bypass a genuine access requirement. If an indispensable integration permission is missing, identify exactly that prerequisite and mark the affected surface BLOCKED_ACCESS. Do not claim access succeeded or route around a blocked tool.

### Required checkpoints and completion report

Give an owner checkpoint after reproduction, after the tested code/PR, after deployment, and after browser acceptance. Each says what changed, what is physically verified, what remains, and whether an owner action is essential. Avoid long silent tool sequences and unsupported claims of background monitoring.

Return the completed matrix with one row per environment/surface and state:

```text
DOCUMENTATION_REF=
SOURCE_BASE_SHA=
REPAIR_COMMIT_OR_PR=
FILES_CHANGED=
FRONTEND_IDENTITIES_BY_SURFACE=
BACKEND_RUNTIME_IDENTITY=
API_SCHEMA_AND_MODE=
SHARED_AUTHORIZED_BACKEND_PARITY=
CANONICAL_ID_MAPPING=
STORAGE_READ_MODEL_PARITY=
CLIENT_DOCUMENT_FACT_REPORT_UI_PARITY=
LATEST_REPORT_VERSION_AND_DOWNLOAD_HASHES=
STRUCTURED_FINDINGS_VISIBLE=
LEARNING_AND_CAPABILITY_AUTHORITY_VISIBLE=
NEGATIVE_AUTH_ERROR_CACHE_AND_READ_SIDE_EFFECT_TESTS=
COMPANY_1_JOB_AND_EXTRACTION_ATTEMPT_PRESERVED=
ACADEMY_AND_COHORT_STATE=
PERSISTED_EVIDENCE_REFERENCES=
STUDIO_PREVIEW_BROWSER_RESULT=
GOOGLE_SHARED_OR_PUBLISHED_BROWSER_RESULT=
ZEABUR_UI_BROWSER_RESULT=
UNRESOLVED_FINDINGS=
OWNER_VISIBLE_INTEGRATION_VERDICT=
```

Only use PASS for owner-visible integration when every required surface has physical evidence at the correct scope and no material parity/security/read-side-effect defect remains. Use PARTIAL, FAIL, BLOCKED_ACCESS, or NOT_EXECUTED truthfully for other results. Do not report a general autonomous CPA readiness verdict from this visibility task.

---

## General program directive — retained reference, not an additional active authorization

Use this document as the compact implementation instruction for Google/Gemini after it has read the full Eve operating-system documentation in this directory.

### Directive

Read, understand, and follow every document in `docs/eve-operating-system/` in numeric order before making changes.

Treat those documents as authoritative design intent for Eve's document intelligence, information custody, knowledge graph, entity resolution, clarification, Academy, UI/data lineage, and verification architecture.

Do not assume that the current runtime already satisfies the documents merely because interfaces or similarly named services exist.

Your task is to reconcile the current implementation against the design, preserve working systems, close measured gaps, activate the resulting process, and prove it with runtime/product evidence.

### Required behavior

1. Inventory the current production code paths and stores first.
2. Trace at least one representative complex document end-to-end.
3. Identify where information is currently stored temporarily and where it can be lost.
4. Implement or reconcile the Universal Document IR and stable source-element identity.
5. Implement or reconcile scoped Information Custody Ledgers and transactional handoffs.
6. Ensure agents consume persisted references and create persisted outputs rather than passing lossy summaries as the only handoff.
7. Preserve every detected source element with an explicit disposition.
8. Build observations, atomic DataPoints, relationships, semantic assertions, verified facts, canonical facts, derivations, presentations, and reports as distinct layers.
9. Preserve project/engagement/entity isolation and anti-silent-merge rules.
10. Route material ambiguity through Professional Clarification rather than guessing.
11. Keep real customer work distinct from synthetic Academy/customer-journey work in both backend and UI.
12. Wire authoritative stores to the actual Eve dashboard, Document Intelligence, Data Point Explorer, Entity Graph, Clarification Hub, financial statements, charts, and reports.
13. Update Minerva/Academy to test information conservation, recall/completeness, entity ambiguity, company reconstruction, product journeys, and learning.
14. Keep Hermes work-conserving and resource-aware without creating artificial CPU load.
15. Preserve historical results. New extraction versions must not rewrite old shallow runs.

### Specific extraction rules

- Do not use predetermined fact-count targets.
- Do not claim a document is deeply understood because a small set of headline metrics is correct.
- Do not call source elements noise and erase them; classify and preserve them.
- Do not collapse thousands of lower-level objects into a summary that replaces the originals.
- Do not allow temporary agent/worker memory to be the only copy of information.
- Do not treat XBRL wrappers, raw chunks, duplicates, and independently meaningful semantic assertions as the same thing.
- Do not infer product correctness from code alone.

### Required proof

For a representative real public-source complex filing, produce an extraction reconciliation showing:

- source artifacts
- container source elements
- leaf source elements
- tables/rows/cells
- XBRL occurrences
- paragraphs/footnotes/visuals/cross-references
- observations/attributes
- evidence occurrences
- atomic DataPoints by family
- relationships by type
- semantic assertions by class
- verified facts
- canonical facts
- unresolved/review-required elements
- information-custody handoff counts at every stage
- source-side coverage/recall denominators
- company reconstruction results

For each stage transition report:

`expected inputs / received inputs / dispositioned inputs / unaccounted inputs`.

Certified transitions require unaccounted inputs = 0.

### Product proof

Use the actual Eve product for product certification. Reconcile visible client, engagement, project, document, graph, clarification, report, and learning data to authoritative stores.

Distinguish proof levels:

- CONFIGURED
- RUNTIME_VERIFIED
- PRODUCT_VERIFIED
- BROWSER_VERIFIED

Do not report a stronger level than was actually observed.

### Continuous operation

After implementation and controlled verification, leave Hermes running under normal resource-aware continuous scheduling. Do not manually manufacture dozens of cases for a favorable report.

If a meaningful long observation window has not elapsed, say so rather than fabricating proof.

### Final report

Return one evidence-based report containing:

- what existed before changes
- what changed
- exact stores/code paths involved
- extraction/custody reconciliation
- UI/backend reconciliation
- Academy/Minerva integration
- continuous scheduler state
- unresolved gaps ranked P0-P3
- strongest proof level for each major capability

Possible final status:

- `PASS` only if deep extraction, zero-loss custody, scoped graph, product wiring, and continuous operation are all proven at the appropriate level.
- `PARTIAL` when architecture is implemented but live evidence remains incomplete or measurable gaps remain.
- `FAIL` when material information can still disappear, customer/project scope is unsafe, or product-visible data cannot be reconciled to authoritative evidence.
