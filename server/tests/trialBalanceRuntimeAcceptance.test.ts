import assert from 'node:assert/strict'; import fs from 'node:fs'; import path from 'node:path'; const dir=process.env.TRIAL_BALANCE_RUNTIME_EVIDENCE_DIR||'/tmp/eve-trial-balance-runtime';const ledger=JSON.parse(fs.readFileSync(path.join(dir,'runtime-ledger.json'),'utf8'));const worker=JSON.parse(fs.readFileSync(path.join(dir,'worker-physical.json'),'utf8'));assert.equal(ledger.marker,'LEDGER_TRIAL_BALANCE_RUNTIME=PASS');assert.equal(worker.marker,'WORKER_TRIAL_BALANCE_PHYSICAL=PASS');assert.equal(ledger.balancedStatus,'SOURCE_TRIAL_BALANCE_BALANCED');assert.equal(ledger.unbalancedStatus,'SOURCE_TRIAL_BALANCE_UNBALANCED');assert.equal(ledger.fallbackStatus,'ACCOUNT_LINES_DISCOVERED_TRIAL_BALANCE_NOT_TESTED');assert.equal(worker.unbalanced.status,'COMPLETE_REVIEW_REQUIRED');fs.writeFileSync(path.join(dir,'runtime-acceptance.json'),JSON.stringify({marker:'P2_LEDGER_TRIAL_BALANCE_RUNTIME_INTEGRATION=PASS',ledger,worker},null,2));console.log('P2_LEDGER_TRIAL_BALANCE_RUNTIME_INTEGRATION=PASS');
