name: Company 1 structured real-agent contracts

on:
  push:
    branches:
      - fix/company1-structured-agent-contracts

permissions:
  contents: write

jobs:
  repair-and-validate:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: fix/company1-structured-agent-contracts
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
      - name: Apply structured real-agent contract repair
        run: python3 .github/scripts/company1_structured_agent_contracts.py
      - name: Install dependencies
        run: npm ci
      - name: TypeScript lint
        run: npm run lint
      - name: Full regression suite
        run: npm test
      - name: Production build and hashes
        run: |
          npm run build
          sha256sum dist/server.cjs dist/worker.cjs
      - name: Commit validated repair and remove one-shot helpers
        shell: bash
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git rm .github/workflows/company1-structured-agent-contracts.yml .github/scripts/company1_structured_agent_contracts.py
          git add -A
          git commit -m "fix(company1): enforce structured real-agent output contracts"
          git push origin HEAD:fix/company1-structured-agent-contracts
